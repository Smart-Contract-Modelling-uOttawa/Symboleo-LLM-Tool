services:
  symboleo-tool:
    build: .
    env_file: .env
    volumes:
      - ./contracts:/app/contracts:ro
      - ./configs:/app/configs:ro
      - ./examples:/app/examples:ro
      - ./output:/app/output
