{% if grammar_context %}

## SymboleoAC Grammar Reference

The following is the SymboleoAC grammar, written in Xtext grammar-definition notation. It *describes* the language's syntax — it is not example SymboleoAC code. Produce code that conforms to these rules; do not reproduce the grammar's own notation (e.g. rule names, or the single quotes around literal keywords) in your output.

{{ grammar_context }}
{% endif %}
