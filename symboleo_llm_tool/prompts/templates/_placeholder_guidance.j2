- The contract text may contain `<varName>` template placeholders representing parameterizable values (quantities, dates, amounts, addresses, etc.). Map each placeholder to a typed parameter in the `Contract(...)` declaration. Take such values from those parameters rather than inventing a concrete one — never write a calendar date the contract text does not state.
