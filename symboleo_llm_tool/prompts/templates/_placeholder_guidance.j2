- The contract text may contain `<varName>` template placeholders representing parameterizable values (quantities, dates, amounts, addresses, etc.). Map each placeholder to a typed parameter in the `Contract(...)` declaration.
